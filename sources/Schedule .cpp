#include "Schedule.hpp"
#include vector

vector<game> leader(int g){
    vector<game> ans;
    for (int t=0; t<g; t++){
     ans.push_back(this->a_sum[i]);
    }
}


int s_k(){
   int ans=0;
   for (int t=0; t<this->a_sum.size(); t++){
     if(this->a_sum[i]->difference>0){
         ans++;
     }
    }  
    retuen ans;
}